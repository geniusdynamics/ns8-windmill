#
# Copyright (C) 2022 Nethesis S.r.l.
# SPDX-License-Identifier: GPL-3.0-or-later
#

[Unit]
Description=Podman  windmill_worker_native-app.service
BindsTo=windmill.service
After=windmill.service db-app.service

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
EnvironmentFile=%S/state/environment
EnvironmentFile=-%S/state/smarthost.env
WorkingDirectory=%S/state
Restart=always
TimeoutStopSec=70
ExecStartPre=/bin/mkdir -p tmp
ExecStartPre=/bin/rm -f %t/windmill_worker_native-app.pid %t/windmill_worker_native-app.ctr-id
ExecStartPre=-runagent discover-smarthost
ExecStart=/usr/bin/podman run --conmon-pidfile %t/windmill_worker_native-app.pid  --cidfile %t/windmill_worker_native-app.ctr-id --cgroups=no-conmon  --pod-id-file %t/windmill.pod-id --replace -d --name  windmill_worker_native-app  --volume workerlogs:/tmp/windmill/logs:Z   --env-file windmill_worker_native.env  ${${WM_IMAGE}_IMAGE}  
ExecStop=/usr/bin/podman stop --ignore --cidfile %t/windmill_worker_native-app.ctr-id -t 10
ExecReload=/usr/bin/podman kill -s HUP windmill_worker_native-app
SyslogIdentifier=%u
ExecStopPost=/usr/bin/podman rm --ignore -f --cidfile %t/windmill_worker_native-app.ctr-id
PIDFile=%t/windmill_worker_native-app.pid
Type=forking

[Install]
WantedBy=default.target
